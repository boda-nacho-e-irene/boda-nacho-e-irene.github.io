- Avoid running in browser
- Use color #464E47 for text
- use color #4BB9EC, #FFA600, #8FC243 for details
- USe color #F0DAB6 for background
- every new section must be added as a link to the lateral navigation menú
- La paleta y la tipografía que manda es la del `:root` de `index.html`: ni un
  color ni una fuente escritos a mano fuera de ahí. Los colores de arriba no son
  los que usa la hoja hoy (la hoja va en piedra y ocre); están en el laboratorio
  como preset «AGENTS.md». Hay que decidir cuál de los dos es el bueno.
- Para probar colores y letras: `index.html?lab`. El panel vive en `lab/` y solo
  baja con ese parámetro. Está documentado en el README.
